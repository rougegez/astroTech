import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['images.pexels.com', 'source.unsplash.com', 'cdn.pixabay.com', 'cdn.mos.cms.futurecdn.net', 'cdn.store-assets.com', 'cdn.shoplightspeed.com', 'physicsworld.com'],
  },
};

export default nextConfig;
